# 校园版协作配置原型

入口：http://127.0.0.1:8765/ （本地服务运行时可用）。

启动：`python3 -m http.server 8765 --bind 127.0.0.1`。

- 原始 `飞象老师校园版.html` 与附件资源保留未修改。
- `index.html` 保留保存网页的渲染内容，移除顶层原应用初始化脚本，在文件预览工具栏增加协作入口。
- `collaboration.css`、`collaboration.js` 实现独立协作配置弹窗。
- 支持示例成员搜索、多选批量添加、权限调整及移除；同校持链接可查看，指定协作者可评论或编辑原件；下载和创建副本默认允许；示例链接复制。
- 设置自动保存在当前浏览器。无后台、实际邀请、身份验证、权限执行或多人同步，不共享原始 AI 对话。

验收：按钮位置与弹窗居中；成员添加、修改和移除；协作者评论／编辑权限切换；刷新保留设置；关闭和重开；复制反馈。

后续可在此基础上继续设计真实协作者页面、评论和编辑流程。

快捷选择：学校内全部成员／同学科全部成员批量追加到待选名单，排除已有协作者；保留手动取消选择及统一赋权添加。同学科按当前邀请者任教学科判断，本地示例为语文；示例目录包含语文、数学和英语教师。不自动纳入未来新增成员。

2026-09-16 弹窗 UI/UX 优化：邀请区与成员管理分区；已选人数、清空选择、权限解释、独立列表滚动和固定底部链接操作；成功操作 toast；支持方向键选择搜索结果、Enter 勾选和 Esc 先关闭候选列表。沿用暖白／绿色配色，补充小屏适配和减少动态效果支持。已在浏览器验证批量选人、清空禁用态、键盘选择添加、权限说明、空搜索、Esc 与复制反馈。仍为本地交互原型。

2026-09-16 独立课件评论原型：入口 `/share.html`，按用户提供的独立分享页截图重建品牌栏与课件封面，移除「立即体验」，顶部提供「添加评论」和评论列表。原工作台保留，协作弹窗复制链接指向该页面。

- `share.html` / `comments.css` / `comments.js`：点击内容就地评论，位置标记、回复、删除自己的评论、作者解决/重新打开讨论。侧栏默认收起。
- 顶部「体验身份」是原型测试工具：李文静（可评论）、杨金田（作者）、周明（仅查看）。暂未与协作弹窗成员权限关联，也不代表真实身份认证。仅查看身份隐藏讨论及评论入口，作者可解决讨论，所有评论者只能删除自己的发言。
- 评论保存在此浏览器 localStorage，刷新保留，同浏览器同源标签可更新；没有后端或真实跨用户同步。
- 评论模式拦截课件点击；退出后恢复答题。课件内评论记录内容及题目上下文，无法匹配时提示回到对应页面，不附着到另一题。尚不支持自动恢复动态题目位置。
- `courseware.html` 复用原有18题课件，去掉远程编辑器脚本；原始资源未修改。分享页封面根据截图制作，点击开始进入该本地课件。
- 浏览器已验证：点标题发评论、作者回复/解决/重新打开、仅查看隐藏评论、评论答案按钮不触发答题、退出后答题可进入下一题；JS语法检查通过。

2026-09-16 WorkBuddy 参考调整：实测顶部编辑模式、选区工具条、AI 编辑输入、直接评论输入、评论筛选、固定侧栏及评论操作菜单；没有向参考页面提交内容。原型顶部改为紧凑编辑/评论图标，删除常驻模式横条；评论汇总改为课件右上角悬浮入口，可固定侧栏，筛选改下拉，选区为蓝框、位置标记为作者头像。增加陈思远（可编辑）测试身份。编辑模式可选中内容并打开「AI 编辑 / 评论」工具条，AI 输入和反馈仅是交互演示，尚无模型接口或真实修改；评论模式仍可完整发送本地评论。已验证编辑选区转评论、发送与持久化、固定侧栏及不同身份入口。当前仍是本地原型，非真实权限服务。

2026-09-16 正式外观调整：去掉体验身份选择器，顶部明确展示「编辑」「评论」文字及图标，选中模式显示退出操作。当前本地页面上下文为作者杨金田（`body[data-current-user="owner"]`）；非作者按现有协作配置读取可编辑／可评论，未配置默认为仅查看。权限控制入口及编辑操作，收到同源其他标签的权限变更时重新计算。上线仍需接入服务端登录身份与权限校验，本地页面属性不能作为授权依据。浏览器验证编辑入口及选区工具条；权限函数覆盖作者、编辑、评论、未邀请和无效权限五种情况。

2026-09-16 评论驱动修改：体验用户提供的飞象批注编辑示例，验证选区批注暂存为对话附件，体验草稿已清除，未发送 AI 修改。分享页新增当前用户的 AI 修改侧栏；有编辑权限的用户可通过「按此评论修改」引用评论及原文，支持多条去重引用、逐条移除/清空、折叠、补充修改要求，收起对话后本页暂存保留。选区「AI 编辑」也接入同一对话。仅可评论角色不显示修改入口；编辑者也可解决评论。对话没有模型后端，提交时明确提示未改动课件，并保留要求，不生成虚假成功响应。浏览器验证了两条评论进入同一对话及引用信息。

2026-09-16 统一批注入口：顶部只显示「添加批注」，不再显示独立编辑/评论模式。支持点击与拖动框选多个内容块，批注记录组合选区的各个锚点、原文和题目上下文。仅评论角色提交「发表评论」；有编辑权限时提交「发送给 AI」，保存批注并直接带入修改对话、尝试提交（无 AI 后端时明确提示并保留要求）。不自动标记评论已解决。浏览器验证作者点击批注到 AI 对话的路径；框选逻辑已实现，尚未完成真实指针拖拽验收。

2026-09-16 批注双操作：有编辑权限时输入框并列显示「确认」「发送给AI修改」。「确认」只保存评论；后者保存评论并携带选区、评论进入左侧修改对话／右侧文件预览。仅可评论角色仅显示确认，提交处理也检查编辑权限。空内容禁用两个按钮，快捷键默认确认。浏览器分别验证确认不打开对话，以及发送给AI打开分栏并带入正确批注。AI 后端仍未接入。

2026-09-16 评论确认后修改：草稿输入框仅保留「确定」勾选按钮；确认保存后在选区附近展示评论详情卡片（原文引用、作者、时间、评论、回复框）。仅有编辑权限时卡片底部展示「按照评论修改」，点击带入左对话／右文件布局，不自动发送或改写。评论标记也可打开此详情卡片。浏览器验证确认后卡片及进入修改对话，两步均成功；AI 后端仍未接入。

2026-09-16 返回原工作台：从分享页点击「按照评论修改」将注释暂存到 sessionStorage 并进入原 `index.html`，不再打开简化的 AI 侧栏。原工作台输入框上方显示 N 条注释，支持 hover/键盘焦点/点击展开原文及建议、移除引用，继续输入修改要求。文件预览的旧编辑图标改为气泡加号「添加注释」。预览内复用分享页批注交互，postMessage 校验来源后把新增引用带回父页输入区；默认显示原有练习，来自封面的批注保留对应封面。当前是作者视角，未连接实际登录身份、模型或多人后端；发送不伪造成功，保留输入。浏览器验证分享评论跳转、数量及引用展开、继续输入不丢失、工作台添加注释入口。预览服务已恢复。

2026-09-16 注释按钮与收起行为：工作台添加注释改为 34px 浅灰圆角按钮、20px 居中气泡加号，补充 hover/选中/键盘焦点样式。确认评论后自动关闭输入浮层及详情/列表，保留位置标记并提示已保存；点击标记仍能查看并按评论修改。已在工作台嵌套课件内验证提交后输入框和详情卡片均隐藏。

### 评论编辑与删除（v10）
- 评论详情和评论列表统一提供本人评论/回复的「编辑」「删除」。
- 编辑支持保存、取消、空内容禁用保存，并标记「已编辑」。
- 删除前确认；删除主评论同时删除回复和位置标记，更新评论数量。
- 再次带入同一评论时，用最新内容更新对话框内的注释引用。
- 验证：浏览器已验证新增后折叠、重新编辑、刷新后保留；删除确认弹窗触发后浏览器控制超时，完整删除流程未完成浏览器验证。

## GitHub 版本入口

- `index.html`：对话工作台与协作设置。
- `share.html`：分享页，支持批注和进入编辑对话。
- `works.html`：我的作品，包含「我的协作」分类。

运行 `python3 -m http.server 8765 --bind 127.0.0.1`，打开 `http://127.0.0.1:8765/`。
仓库排除了原站网页快照与不参与运行的原站 JavaScript bundle，保留原型所需样式和图片。
当前为本地交互原型，成员、评论保存在浏览器中，尚未接入登录鉴权、多人同步或 AI 修改服务。
